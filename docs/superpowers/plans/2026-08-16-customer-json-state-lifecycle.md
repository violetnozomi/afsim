# 甲方 JSON 接入与状态生命周期实施计划

1. 增加网络身份与 Codec 回归测试，修复同类型多网络汇总和引用校验。
2. 增加消息生命周期纯单元测试，实现去重、乱序、新运行和退休运行处理。
3. 增加快照合并纯单元测试，实现资源域保留、导航 upsert、环境合并和版本递增。
4. 扩展环境数据结构、Codec 和参数化影响适配器，覆盖 Schema 中现有环境字段。
5. 修复规划配置版本绑定，保持统一接纳入口。
6. 接通 `assessment_request`，统一界面与外部 JSON 的评估入口，并处理快照变化后的缓存失效。
7. 保存 Provider 握手状态和最近标准响应，补充可观测性。
8. 更新架构、接口、使用流程、里程碑和会话交接文档。
9. 运行静态守卫、全量构建测试、Shell 回归、Schema/部署契约和 25 节点场景；对失败项逐一修复。

每一步只修改插件目录和测试，不修改 AFSIM 核心，不提交 Git commit。
