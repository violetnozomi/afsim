# 甲方进程内接口验收闭环实施计划

1. 先扩充 `CustomerJsonCodecTest`，构造两个 LINK16 网络、各两个端点和一条链路，验证隔离及跨网引用拒绝；随后实现明确 `networkId`。
2. 扩充生命周期测试，验证同运行资源更新后导航/环境保留、导航按 `platformId` upsert；随后补齐明确字段和兼容回退。
3. 扩充环境测试覆盖三种 `applicationMode` 及不同证据；随后实现三态语义。
4. 为 JSON codec 注入拒绝型校验器，先证明所有解码入口受统一校验层控制，再实现默认校验抽象。
5. 新建 DataContainer 端到端测试，覆盖实际文件入口、评估响应、活动配置绑定和派生状态失效。
6. 更新自动化守卫、接口说明、验收映射、当前里程碑和会话交接。
7. 执行目标测试、完整回归、静态守卫、甲方合同样例验证、WSF/Warlock 构建、部署契约与典型场景。
