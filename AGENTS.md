# AGENTS.md — AFSIM Network Resource Manager

本文件是所有开发AI的强制入口，适用于仓库根目录及全部子目录。

## 1. 每次会话必须执行

开始任何工作前，按顺序完成：

1. 阅读本文件。
2. 阅读docs/ai/PROJECT_MEMORY.md。
3. 阅读docs/ai/CURRENT_MILESTONE.md。
4. 阅读docs/ai/DECISIONS.md。
5. 阅读docs/ai/SESSION_HANDOFF.md。
6. 阅读docs/NEXT_DEVELOPMENT_INSTRUCTIONS.md。
7. 运行scripts/ai_guard.sh status。
8. 用一句话声明本次唯一目标、允许修改的文件和完成标准。

没有完成上述步骤，不得修改代码。

## 2. 项目主线

项目主线是：在不修改AFSIM核心源码的前提下，实现可加载、可回退、可追踪的网络资源管理插件，完成通信资源采集、指标计算、需求匹配、网链评估、规划建议、甲方模块适配和合同验收证据闭环。

当前工作必须直接服务合同3.2.1至4.2。不能因为某个新算法、实验结果或界面效果有趣而改变主线。

## 3. 单任务锁定

一次只允许一个明确任务处于进行中。

开发AI必须：

- 使用CURRENT_MILESTONE中的当前任务；
- 先完成当前任务的实现、测试和文档；
- 当前任务未完成时，不得顺手开发下一模块；
- 发现新问题时记录到SESSION_HANDOFF，不立即扩展范围；
- 需要改变目标时，先停止并请求用户确认。

禁止把“顺便优化”“进一步探索”“效果不好继续调参”当成合理扩展。

## 4. 测试与实验的边界

测试用于验证固定需求，实验用于搜索方案。两者必须严格区分。

允许自主运行：

- 编译受影响目标；
- 单元测试；
- 固定回归测试；
- CURRENT_MILESTONE明确列出的固定验收场景；
- 一次修改前基线和一次修改后验证。

未经用户明确要求，禁止：

- 参数扫描；
- 网格搜索；
- 多seed性能试验；
- 反复调整阈值直到结果变好；
- 比较未列入当前任务的新算法；
- 新建研究分支；
- 长时间批量仿真；
- 因测试失败而改变合同指标或验收口径。

测试失败时先定位代码、数据契约或环境问题，不能通过调参数掩盖失败。

连续两次修复仍遇到相同阻塞时，停止扩展修改，在SESSION_HANDOFF记录错误、已尝试方法和下一步所需输入，并向用户汇报。

## 5. 事实与状态规则

以下状态含义固定：

- MAPPED：已有需求、设计和字段映射。
- IMPLEMENTED：代码与单元测试完成。
- VERIFIED：固定AFSIM场景和验收证据完成。
- PRE_ACCEPTANCE：使用内部配置、模拟接口或冻结代表样例。
- FINAL_ACCEPTANCE：使用甲方正式接口、样包和目标环境。

不得根据CHANGELOG、代码存在或单次运行成功直接宣称VERIFIED。

任何缺失或不可靠数据必须：

- valid=false；
- 携带固定原因码；
- 保留来源和置信度；
- 不参与满足／不满足的正向判定。

禁止把缺失值填0后标记有效。

## 6. 架构不可破坏项

必须保持：

- 不修改AFSIM核心目录；
- 公共契约include/nrm不依赖Qt和AFSIM；
- 仿真回调只复制、关联和计数；
- 文件I/O不在仿真回调执行；
- GUI只消费值对象，不持有AFSIM裸指针；
- 外部协议结构只存在于适配层；
- 当前图、实际路径和参数化候选图语义分离；
- 参数化数据标记来源与置信度；
- 自动建链、改频和改路由不属于当前只读插件；
- 已有公共字段和InputProvider签名保持兼容。

如任务确实要求破坏其中一项，必须先提交设计说明并获得用户确认。

## 7. 导航职责边界

甲方导航功能包负责GNSS／INS解算。

本项目只负责结果包接入、版本和完整性校验、字节序／单位／坐标标准化、精度和误差字段映射、状态管理、展示、记录和转发。

不得自行实现导航算法，不得猜测甲方二进制格式。甲方接口缺失时只实现模拟适配器并标记PRE_ACCEPTANCE。

## 8. 命令规则

### 8.1 允许的只读命令

优先使用rg、sed、nl、find、git status、git diff、git log、git show和scripts/ai_guard.sh status。

### 8.2 允许的验证命令

优先使用：

- scripts/ai_guard.sh static
- scripts/ai_guard.sh test
- scripts/ai_guard.sh check
- scripts/ai_guard.sh scenario 指定场景名

场景命令只允许脚本内列出的固定场景，并且每次只运行一次。

### 8.3 需要用户明确批准

以下操作执行前必须征得用户明确同意：

- sudo或root操作；
- 安装系统包或第三方依赖；
- 网络下载；
- 修改systemd服务；
- 修改AFSIM核心源码；
- 运行超过两分钟的仿真；
- 批量运行场景；
- 修改合同指标、公式或统计口径；
- 提交git commit、创建tag、推送远程；
- 删除用户文件或批量移动目录。

### 8.4 禁止命令

禁止执行：

- git reset --hard
- git clean -fd或git clean -fdx
- git checkout -- 路径
- git restore 路径
- rm -rf
- git push --force
- 未经批准的curl、wget、pip install、apt、yum
- 自动参数搜索脚本
- 无上限循环运行测试或仿真

不得删除或覆盖不是本次会话创建的改动。发现并行AI或用户改动时，必须保留并继续兼容。

## 9. 文件修改规则

- 修改前先阅读目标文件和直接依赖。
- 手工修改使用apply_patch或等价补丁工具。
- 不创建.orig、.rej、.bak临时文件留在仓库。
- 不提交output、日志、截图、构建目录或临时产物，除非验收明确要求。
- 不做无关重构、批量格式化或大范围改名。
- 新增公共类型必须有独立测试。
- 修改输出字段必须同步schema、测试和文档。
- 修改版本必须同步VERSION、Version.hpp、CHANGELOG和运行文档。

## 10. 完成前检查

每次交付前必须：

1. 运行scripts/ai_guard.sh static。
2. 运行受影响测试；公共契约或核心逻辑变化时运行scripts/ai_guard.sh test。
3. 检查git diff，只包含当前任务相关文件。
4. 更新CURRENT_MILESTONE状态。
5. 更新SESSION_HANDOFF。
6. 说明未运行的验证和剩余风险。

没有测试证据时只能报告“已实现，未验证”。

## 11. 结果汇报格式

汇报必须包含本次唯一目标、修改文件、行为变化、测试命令和结果、合同追踪键状态变化、外部阻塞以及下一步唯一建议。

不得用“效果不错”“应该可以”等模糊表述代替证据。
